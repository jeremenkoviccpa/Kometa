# Validation thresholds (spec section 9). Fence: learning and research can never edit this file.
walk_forward: {train_years: 3, test_months: 6, step_months: 6, search_budget: 20, min_train_trades: 10}
holdout: {months: 12, max_attempts_per_version: 1, max_attempts_per_family_per_epoch: 5}
monte_carlo: {runs: 5000, method: block_bootstrap_trades, block_length: auto}
stability: {param_shift: 0.20}
cross_market: {min_pairs_passing: 3, of_pairs: 5}
thresholds:
  min_oos_trades: 200
  min_profit_factor: 1.3
  min_deflated_sharpe_prob: 0.95
  max_mc_dd_p95_at_half_percent_risk: 0.12
  min_stability_profit_factor: 1.1
  min_holdout_profit_factor: 1.1
  suspicious_monthly_return: 0.10
