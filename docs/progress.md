# Build progress

Machine-read by tests/unit/test_process_rules.py: a phase marked `done` must have docs/retros/phase_<n>.md.

| Phase | Status | Notes |
|---|---|---|
| 0 | done | Workspace, Makefile, CI, compose (validated, not started: Docker daemon was down), settings, instruments, Alembic baseline, CLAUDE.md. MT5-on-Windows check is open question 9. |
| 1 | done | Synthetic gate passed: models, indicators (incl. S/R levels, geometry, sessions, event index) with parity + poisoning tests, synthetic generator with fault injection, quality checks, data_version, resampling, file source, calendar sources, spread stats. CARRY-OVER (blocked): Postgres writer/reader for bars_m1 and continuous aggregates (Docker not running here), MT5 history source (needs the Windows bridge, Phase 5), real 10-year load (open question 2). |
| 2 | done | Event loop, SimClock, MarketView (copies only), strategy API + manifest, AST static checks, loader, SimBroker (full cost model, M1 resolution, event-jump), demo_ma_cross, golden backtest, poisoning test (catches a cheating strategy), perf: 10y M1 + H1 in ~35 s. |
| 3 | done | Walk-forward with seeded search, locked holdout with epochs and family budget, block-bootstrap Monte Carlo, stability, cross-market, DSR (hand-checked), append-only hash-chained trial registry, JSON + HTML reports, `at validate`. Real-data reports wait for open question 2. TODO: process-pool parallelism for search. |
| 4 | done | Signed risk.yaml (Ed25519, owner key), all 9 checks in spec order with independent Decimal sizing, halt states persisted atomically (corrupt state -> FULL_HALT), loss references per v1.1, owner-signed single-use resume tokens, Ed25519-signed decisions with sequence + expiry verified by core.signing.DecisionVerifier (execution side), `at risk keygen/sign/verify/resume-token`. Property tests: approved <= proposed, no approval without valid stop, none in any halt, total/strategy/trade risk within limits (approval-biased generator), tampered/missing/foreign signature refuses startup. Not yet: running as a Redis-bus service (wired in Phase 5/7 integration), in-process adapter for backtests. |
| 5 | done | Code and chaos gate done: BrokerAdapter + FakeBroker (fault injection), MT5 bridge (FastAPI, token auth, server-time conversion, demo/real guard) + MT5Adapter (HTTP), order manager (signed-decision check, write-ahead journal, idempotent client ids, stop confirmation, expiry, tighten-only, halt actions), reconciliation (RECON_HALT, evidence-only resync, owner resync, external positions, cash check), watchdog, execution quality log, startup checks, `at execution check`, migration 0003. Chaos: named scenarios + random fault schedules + 48 h simulated soak; `make mutate` (14/14). ACCEPTANCE GATE STILL OPEN: the real 48 h paper run on a broker demo account waits for open questions 1 (broker) and 9 (Windows VPS); run `at execution check` then the soak against the bridge. Not yet: Redis transport between services (Phase 7 integration), Postgres writers for the execution tables. |
| 6 | done | Live bar builder (shared bucket alignment with historical aggregation, DST-tested), live runner (same request checks as backtest), ShadowBroker + ShadowSession + `run_shadow` loop, backtest/shadow signal parity on a recorded quote stream, lifecycle registry (hash-chained ledger, state machine, illegal transitions alert), evaluator (shadow band exit, micro/live exits, weekly promotion cap, drawdown/PF/slippage/drift demotions, retire on second demotion), demotion actions in execution, BacktestProfile from validation, `at lifecycle submit/status/retire`. Acceptance: demo_ma_cross reaches shadow on live quotes and stays capped (the risk gate refuses it; the same intent from a live version passes); every demotion scenario tested. Not yet: trade journal writing real (micro+) trades from execution deals (Phase 7 wiring), Postgres-backed registry. |
| 7 | done | Allocator (shrunk Sharpe, correlation slots, 30% slot cap, stage caps, micro fraction, weekly rebalance, persisted, one intent per signal) with property tests; message schema + bus (in-memory and Redis Streams consumer groups, ack after handling, redelivery after a crash, poison messages acked and reported); services on the bus: engine-live, allocator, risk gate (signed rejections without fresh account data, halt re-published on start), execution (decisions, halts, demotion orders, ownership-checked strategy requests, account updates, closed trades with R), lifecycle (trades and shadow signals in, stage changes and demotion orders out). Acceptance: one simulated day+ of the full pipeline over both buses, with a loss halt and a live->micro demotion taking effect end to end. Not yet: service entry points and compose services (with deployment, Phase 11), scheduler jobs (rollover, weekly rebalance, hourly evaluation) as a service. |
| 8 | done | Audit log: hash-chained JSON Lines and Postgres (`audit_log`, trigger refuses UPDATE/DELETE/TRUNCATE, app role INSERT/SELECT only, canonical text kept, advisory-locked appends), `at audit verify [--db]`, every decision-relevant stream audited incl. a new `orders` stream (placed/modified/filled/cancelled) and ConfigChanged hashes of every config file read. Alerts: Telegram + email fallback, criticals repeat every 10 min until `/ack` from the owner chat; every spec critical fires in one test; warnings for demotion, slippage above the cost model, data quality on traded symbols. Daily summary. API (read-only views, owner-token audited controls, no risk-limit endpoint) + live dashboard page; `at demo run` composes the whole system. Grafana: the 7 spec dashboards generated from scripts/dashboards.py, every panel run as the read-only role on real writer output, verified in a live Grafana. Migrations 0004-0006 applied on real TimescaleDB for the first time. Not yet: rollback and API-budget warnings (Phase 9 loops), the scheduler running the daily summary / chain check / quality alerts as jobs and the monitor service entry point writing to Postgres (Phase 11), Postgres writers for bars/orders/execution_quality (carry-over, now unblocked). |
| 9 | todo | |
| 10 | todo | |
| 11 | todo | |

## Owner requests outside the phase order (2026-09-25)
- Trading hub (API `/`): overview, live candles (Lightweight Charts), TradingView reference chart, trade
  journeys, research, risk, execution, alerts, audit and guide tabs; demo trades XAUUSD realistically.
- ForexFactory calendar feeds the risk gate's news blackout on a real clock (open question 31 for staleness).
- Dukascopy history fetcher (`at data fetch`); the feed throttles hard (about 5-10 files a minute), so the
  full gold 2017+ and FX 2019+ download runs for many hours; rerunning the command resumes.
- Strategy library (owner decision): swing_trend_pullback, candle_sr_reversal, scalp_session_breakout as
  candidates, plus reversal-candle indicators. First look on real gold 2017-2020: none profitable with
  defaults (docs/research_log.md). Next: `at validate` each on the full history.
